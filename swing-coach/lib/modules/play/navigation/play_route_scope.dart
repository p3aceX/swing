import 'package:flutter/material.dart';

class PlayRouteScope extends StatelessWidget {
  const PlayRouteScope({required this.child, super.key});

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
