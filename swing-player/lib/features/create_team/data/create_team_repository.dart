import 'package:flutter_host_core/flutter_host_core.dart';

// Player-side alias over the shared team-creation repository.
typedef CreateTeamRepository = HostCreateTeamRepository;

final createTeamRepositoryProvider = hostCreateTeamRepositoryProvider;
