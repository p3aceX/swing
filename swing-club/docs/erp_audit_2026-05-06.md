# Swing Club ERP Audit (May 6, 2026)

## Critical Findings (Blockers)
1. Session module is built but unreachable: UI navigates to `/sessions/*` routes that are not registered in router.
   - `lib/features/sessions/session_list_screen.dart:63`
   - `lib/features/sessions/session_list_screen.dart:190`
   - `lib/router.dart`
2. `MoreScreen` is disconnected and points to non-existent `/more/*` routes.
   - `lib/features/more/more_screen.dart:69`
   - `lib/router.dart`
3. Legacy batch flow fails static analysis (`dart analyze lib`): `create_batch_flow.dart` has API mismatch and undefined symbols.

## Page-by-Page ERP Readiness

### Auth pages
Routes: `/splash`, `/phone`, `/name`, `/otp`, `/business-details`, `/academy-setup`
- Status: Connected
- Missing: role selection, branch selection, onboarding checkpoints, terms acceptance persistence, device/session management

### Home (`/home`)
- Status: Connected
- Missing: operational schedule center, exceptions queue, coach allocation conflicts, task workflows

### Batches (`/batches`, `/batches/new`, `/batches/:id`, `/batches/:id/edit`)
- Status: Connected
- Missing: calendarized planning, conflict checks, waitlist automation, archive/versioning, audit trail

### Students (`/students`, `/students/:enrollmentId`)
- Status: Connected
- Missing: admission pipeline, documents/KYC, guardian model, medical notes, transfers, reactivation workflows

### Play (`/play` + child routes)
- Status: Connected
- Missing: linkage to academy coaching outcomes and player development plans

### Finance (`/payments`)
- Status: Connected
- Missing: invoicing lifecycle, reconciliation, GST/tax reports, payroll integration, ledgers/account heads, month close, export standards

### Coaches (`/coaches`, `/coaches/:id`)
- Status: Connected
- Missing: shift/attendance, leave, payroll contracts, certifications, utilization/performance tracking

### Announcements (`/announcements`, `/announcements/create`)
- Status: Connected
- Missing: scheduled sending, channel delivery integration, read receipts, segmentation rules, templates

### Inventory (`/inventory`)
- Status: Connected
- Missing: transaction history (inward/outward), vendor mgmt, reorder points, depreciation, assignment tracking, stock audits

### Settings (`/settings`, `/settings/profile`, `/profile`)
- Status: Connected but duplicated profile entry points
- Missing: org-level controls, RBAC management, policy controls, integration settings

## Not Connected / Incomplete Routes
1. Missing session routes: `/sessions/report`, `/sessions/:id`
2. Missing all `/more/*` routes used by `MoreScreen`
3. No explicit 404 handling route

## Cross-Cutting ERP Gaps
1. No RBAC matrix
2. No audit logs
3. No multi-branch model
4. No reporting/export center
5. No approval/escalation workflow engine
6. Very low test/documentation maturity

## Suggested Priority
1. Route integrity cleanup
2. Remove/repair legacy non-compiling flow files
3. RBAC + audit logging baseline
4. Sessions as first-class ERP module
5. Finance hardening
